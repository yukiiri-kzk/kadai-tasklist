<h1>タスク新規作成ページ</h1>

<%# データーベースに保存する場合、form_withの引数にはモデルクラスのインスタンス｛＝保存したいテーブルクラス（Task）のインスタンス（＠task）｝を指定 %>
<%= form_with(model: @task, local: true) do |f| %>
<%# f.label,f.text_fieldでカラムを指定し、保存したいテーブルクラスの中のインスタンスのどのカラムに対する入力欄なのかを明示（今回はcontentのカラム） %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '作成'%>
<% end %>

<%# link_toの第１引数にはリンクのテキスト、第２引数にはパスやURLを指定 %>
<%= link_to '一覧に戻る', tasks_path %>